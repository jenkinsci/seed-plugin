# Configuration file for the 'Repository credentials' acceptance test

# Repository credentials
seed.dsl.repository = https://artifactory.nemerosa.net
seed.dsl.repository.user = ${ARTIFACTORY_USER}
seed.dsl.repository.password = ${ARTIFACTORY_PASSWORD}
